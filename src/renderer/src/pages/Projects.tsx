import { ReactNode } from 'react'

export default function Projects(): ReactNode {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-2xl font-semibold">Projects</h1>
      <p className="text-muted-foreground">项目列表与管理（占位页面）。</p>
    </div>
  )
}